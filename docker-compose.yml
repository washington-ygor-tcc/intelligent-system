version: '3.9'

services:
  api:
    restart: always
    build: 
      context: .
    environment:
      - APPLICATION_TYPE=REST
    ports:
      - 8000:8000
    volumes:
      - ./:/src
    entrypoint: ['uvicorn', 'src.main:application', '--reload', '--host', '0.0.0.0']
